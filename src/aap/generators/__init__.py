"""Phrase archetypes — the process families a hit instantiates.

FRAGMENT (one event becomes many), CONVERGE (dispersed events collapse toward a
target), PROPAGATE (an event travels through instrumental space), RESONATE (an
attack generates a sustained field), INTERRUPT (a human event terminates a
machine process), SHADOW (the machine recalls a recent human gesture,
transformed).

A hit does not launch a stored sequence; it instantiates a precomposed abstract
process. The same archetype appears differently according to formal state —
that is what creates coherence without literal repetition.

Design notes 7, 10, 18.  Fragment at roadmap Stage 4; the rest at Stage 10.
"""
